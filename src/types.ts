export type Settings = {
  model: string;
  style: string[];
  tone: string[];
  format: string;
  action: string;
  character: string;
  maxTokens: number;
  temperature: number;
  apiKey: string;
};
